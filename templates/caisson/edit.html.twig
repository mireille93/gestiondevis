{% extends 'base.html.twig' %}

{% block title %}Edit Caisson{% endblock %}

{% block body %}
    <h1>Editer Caisson</h1>

    {{ include('caisson/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('caisson_index') }}">back to list</a>

    {{ include('caisson/_delete_form.html.twig') }}
{% endblock %}
