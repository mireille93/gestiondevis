{% extends 'base.html.twig' %}

{% block title %}Accessoire{% endblock %}

{% block body %}
<div class="container">
    <div class="panel panel-primary">
        <div class="panel-heading">
            <i class="icon_menu"> Detail Accessoire</i>
        </div>
        <div class="panel-body">

    <table class="table">
        <tbody>
            <tr>
                <th>Designation</th>
                <td>{{ accessoire.designationace }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ accessoire.typeace }}</td>
            </tr>
            <tr>
                <th>CRO</th>
                <td>{{ accessoire.croace }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('accessoire_index') }}">back to list</a>

    <a href="{{ path('accessoire_edit', {'id': accessoire.id}) }}" class="btn btn-success">edit</a>

    {{ include('accessoire/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
