{% extends 'base.html.twig' %}

{% block title %}Edit Accessoire{% endblock %}

{% block body %}
<div class="container">
    <div class="panel panel-primary">
        <div class="panel-heading">
            <i class="icon_menu"> Editer Accessoires</i>
        </div>
        <div class="panel-body">

    {{ include('accessoire/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('accessoire_index') }}">back to list</a>

    {{ include('accessoire/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
